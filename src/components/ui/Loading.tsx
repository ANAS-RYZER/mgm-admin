const Loading = () => (
  <div className="flex items-center justify-center w-full min-h-[200px]">
    <div
      className="h-8 w-8 animate-spin rounded-full border-2 border-gold-light border-t-transparent"
      aria-label="Loading"
    />
  </div>
);

export default Loading;
